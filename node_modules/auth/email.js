const sender = "testing@mailtrap.io";
const provider = "smtp.mailtrap.io";
const port = 2525;
const auth_user = "c19df9c90f6fd6";
const auth_pass = "ad6ef8478adf89";

const nodemailer = require("nodemailer");

var transport = nodemailer.createTransport({
  host: provider,
  port: port,
  auth: {
    user: auth_user,
    pass: auth_pass
  }
});

function send(address, subject, content) {
    var mailOptions = {
        from: sender,
        to: address,
        subject: subject,
        text: content
    };

    transport.sendMail(mailOptions, function(error, info) {
        if (error) {
            console.log(error);
        } else {
            console.log('Email sent: ' + info.response);
        }
    });
]